project_name: "jellyfin alexa"
default_status: "To Do"
statuses: ["To Do", "In Progress", "Done"]
labels: []
definition_of_done: ["dotnet build passes with 0 errors", "dotnet test passes", "No new compiler warnings introduced", "Session attributes use proper DTOs not raw ValueTuples for serialization", "HttpClient instances are not shared across calls that modify BaseAddress", "NLU test fixtures updated if interaction model changed", "E2E test added for new intent or handler logic", "Locale response strings added to all 17 locales"]
date_format: yyyy-mm-dd
max_column_width: 20
default_editor: "vi"
auto_open_browser: true
default_port: 6420
remote_operations: true
auto_commit: false
bypass_git_hooks: false
check_active_branches: true
active_branch_days: 30
task_prefix: "jf"
