---
id: JF-176.3
title: Integrate carousel into LaunchRequestHandler with APL fallback
status: Done
assignee: []
created_date: '2026-05-18 09:49'
updated_date: '2026-05-18 12:40'
labels:
  - launch
  - handler
dependencies:
  - JF-176.1
  - JF-176.2
references:
  - Jellyfin.Plugin.AlexaSkill/Alexa/Handler/LaunchRequestHandler.cs
parent_task_id: JF-176
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Modify `LaunchRequestHandler` to render the APL carousel when the device supports it and there is playback history.

**Implementation notes:**
- After the existing resume-priority check (lines 71-80), add the APL carousel path
- Call `AplHelper.DeviceSupportsApl(context)` to check capability
- Call the carousel data query (JF-176.2) to get recent items
- If items exist, call the APL carousel template builder (JF-176.1) with the data
- If no items, show a visual welcome prompt
- Always include a voice `OutputSpeech` alongside the visual
- Add locale strings for the welcome/carousel prompt to `ResponseStrings` and all locale JSON files
- Keep `ShouldEndSession = false` so the user can tap or speak

**Depends on:** JF-176.1 (APL template), JF-176.2 (data query)
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 Carousel APL directive included in LaunchRequest response when APL is supported
- [ ] #2 Non-APL devices get current voice-only behavior with no APL directives
- [ ] #3 Resume-offer logic takes priority: if audio was playing, resume prompt is shown instead of carousel
- [ ] #4 Voice speech plays alongside carousel ('What would you like to play?')
- [ ] #5 Session is kept open (ShouldEndSession = false) to allow follow-up interaction
<!-- AC:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Modified LaunchRequestHandler to show APL carousel of recently played items on skill launch. Added IUserManager dependency for Jellyfin user resolution. BuildWelcomeResponseAsync now attaches carousel directive when APL is supported, visuals enabled, and playback history exists. Resume-offer logic takes priority. Added "RecentlyPlayed" locale string to all 17 locales. 5 new tests covering carousel attach, non-APL fallback, no-history case, and resume priority. 1644 tests pass.
<!-- SECTION:FINAL_SUMMARY:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [ ] #1 dotnet build passes with 0 errors
- [ ] #2 dotnet test passes
- [ ] #3 No new compiler warnings introduced
- [ ] #4 Session attributes use proper DTOs not raw ValueTuples for serialization
- [ ] #5 HttpClient instances are not shared across calls that modify BaseAddress
- [ ] #6 NLU test fixtures updated if interaction model changed
- [ ] #7 E2E test added for new intent or handler logic
- [ ] #8 Locale response strings added to all 12 locales
<!-- DOD:END -->
