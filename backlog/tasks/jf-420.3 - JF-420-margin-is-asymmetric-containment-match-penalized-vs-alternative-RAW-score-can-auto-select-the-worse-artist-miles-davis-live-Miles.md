---
id: JF-420.3
title: >-
  JF-420 margin is asymmetric: containment match penalized vs alternative RAW
  score can auto-select the worse artist ('miles davis live' -> 'Miles')
status: Done
assignee:
  - zai
created_date: '2026-08-31 17:20'
updated_date: '2026-09-01 11:55'
labels:
  - code-review
  - artist-search
dependencies: []
references:
  - >-
    Jellyfin.Plugin.AlexaSkill/Alexa/Handler/Intent/PlayArtistSongsIntentHandler.cs:441
  - Jellyfin.Plugin.AlexaSkill/Alexa/FuzzyMatcher.cs
parent_task_id: JF-420
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
New code-review finding (2026-08-31 second high pass, PLAUSIBLE with constructible scenario; verifier confirmed code shape). PlayArtistSongsIntentHandler.cs:441 (the JF-420 margin comparison). Distinct from jf-420.1 (equality bypass does not fix this).

DEFECT: the gate compares the containment match's PENALIZED fair score (ContainmentScore * name/query length ratio) against the alternative's RAW score, with no symmetric penalization and no full-coverage check on the alternative. So a containment-exemption 90 on the ALTERNATIVE side can auto-select the worse artist.

SCENARIO: query 'miles davis live' with 'Miles Davis' and 'Miles' in the library. Tier-2 prefix ties both at 90 (pool row order decides; ArtistIndexService sets no OrderBy). If 'Miles Davis' wins tier 2, the gate's FindBestMatchWithScore gives 'Miles' 90 via the same containment exemption (ApplyLengthPenalty exempts contained candidates), and 90 - 90*11/16 (=61.9) = 28.1 > ContainmentVsFullNameMargin(20) auto-selects 'Miles', discarding the better 'Miles Davis' match the user asked for.

FIX SHAPE: make the comparison symmetric (penalize the alternative's containment-shaped score identically, or drop the exemption for the margin computation) AND require the alternative to cover the full query before it can win by auto-select. Coordinate with jf-420.1 (same code region; land order matters for the tests).
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 The comparison is symmetric: the alternative's score is either length-penalized the same way as the containment match, or the containment exemption is dropped for the alternative side when computing the margin (pick the principled option and document it in code)
- [x] #2 The alternative must have full-keyword coverage of the query to win by auto-select (a partial-word containment exemption alone cannot outrank a better full match)
- [x] #3 Unit test: query 'miles davis live' with 'Miles Davis' and 'Miles' in the library resolves/plays Miles Davis (or prompts), never auto-selects 'Miles'
- [x] #4 No regression on the JF-420 original case ('P!nk floyd' still offers/auto-selects correctly) and on jf-420.1's exact-match bypass once it lands
- [x] #5 ComputeContainmentFairScore no longer drifts from FuzzyMatcher.ApplyLengthPenalty: the 0.5 ratio-floor branch and integer/rounding semantics are aligned (or the matcher's own logic is reused directly), so the margin compares two scores computed by the same rules; regression: query 'the beatles' vs artist 'Beatles' (ratio ~0.64) must not produce a 30+ point phantom margin
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
2026-08-31 third code-review pass added a third aspect + AC: ComputeContainmentFairScore (PlayArtistSongsIntentHandler.cs:55) claims to compute 'what the penalty would apply without the exemption' but omits ApplyLengthPenalty's 0.5 ratio floor and uses different arithmetic, so in the ratio >= 0.5 region the handler's copy and the matcher disagree by 30+ points ('the beatles' vs 'Beatles': 90 unpenalized vs 57.3 computed), which alone can exceed ContainmentVsFullNameMargin and auto-select a tribute act where consistent math disambiguates. JF-420.1's exact-equality bypass does not cover article/ASR-drift near-exact queries; this alignment does.

2026-09-01 implementation complete (all 5 ACs): FuzzyMatcher.ApplyFairLengthPenalty extracted as the exemption-free primitive (ApplyLengthPenalty is now a one-line containment-exemption override on top of it - one definition, drift structurally impossible, kills the 33-point floor-region disagreement); the JF-420 gate compares BOTH sides with fair scores, the alternative must keep fair >= AlternativeFullNameThreshold (80: exemption-only partial-word hits like an artist named 'Live' inside 'beatles live' cannot win), and IsRedundantShorterForm skips the comparison when the alternative is a shorter form of the match (Miles vs Miles Davis), guarded on the match being word-subset of the query so superstring tier-1 matches (tribute acts) never skip.

Empirical detour worth recording: the review's original 'the beatles' scenario is NOT reachable through the inline search (tier 1 matches the tribute band by name-contains-query and the gate never fires); the reachable phantom-margin shape is 'beatles live' with an artist literally named 'Live' (containment-exempt 90 alt vs floor-region match). Diagnosed via a temporary dump test that printed real FindBestMatchWithScore/ApplyFairLengthPenalty values (miles: tier2=Miles Davis 90, alt Miles 90 raw/28 fair; beatles-live: alt Live would be 90 raw/30 fair) plus the missing-SetupUserMock red herring ('User not found' Tell masked the real flow). Dump test deleted after use.

Tests: Miles test (per-artist song mock, asserts 'So What' plays = Miles Davis, both tier-2 row orders covered by the two mechanisms), beatles-live phantom-margin test (disambiguate, not auto-select 'Live'), ApplyFairLengthPenalty unit test pinning floor/truncation/exemption-free semantics. Suite 2789 -> 2792 green; Release build 0 warnings.

2026-09-01 /simplify (4 agents) applied: ComputeContainmentFairScore wrapper deleted (3 agents flagged the argument-transposing one-liner; both gate sides now call the matcher directly, symmetrically readable); AlternativeFullNameThreshold const named (one bar, two checkpoints, documented); IsRedundantShorterForm named predicate (rationale at the decision point, no double negation); Split(' ') house idiom. Skips with reasons: deeper 'competitive alternative' rule would subsume the subset skip but changes enshrined test outcomes (beatles-live disambiguate->auto-play is a UX decision - follow-up candidate); raw>=80 NOT redundant (load-bearing for the else branch, nirvana-unplugged regression); IsWordSubset stays handler-private (single consumer; hoist to ArtistSearch if a second appears); gate stays handler-local (documented JF-382 status quo - the PRIMITIVE went to FuzzyMatcher where everyone can use it); gate requires _artistIndex.IsReady so cold-index plays bypass it (pre-existing, noted).

2026-09-01 DEPLOYED + VERIFIED (DLL cfc36d8, AlexaSkill_0.12.0.0, config 1 user survived, active-DLL MD5 match, both indexes loaded clean). Live proof of the new gate: 'P!nk floyd' logs "containment match 'P!nk' (fair 36) clearly beaten by full-name alternative 'Pink Floyd' (fair 90), auto-selecting (JF-420)" - the bidirectional fair scores matching the unit-test arithmetic exactly. Soul Coughing plays (420.1 intact), xyzzyfoo clean not-found. Note: 'beatles live' on THIS library resolves to 'Eagles' via the fuzzy tier (no 'Beatles'/'Live' artists) - pre-existing search quality for that query shape, not a bundle regression.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
JF-420.3: the JF-420 containment-vs-full-name margin is symmetric, drift-free, and masks no alternative. Closes the JF-420 implementation side (JF-420.2 prompt semantics remain in queue).

WHAT CHANGED (commits 42ad2a3 + 7d4df1b, 9 files)
- FuzzyMatcher.ApplyFairLengthPenalty: the exemption-free length penalty, single-sourced; ApplyLengthPenalty is now a one-line containment-exemption override on top of it. The handler's private formula (which had drifted: missing 0.5 floor, 33-point floor-region error) is gone; structural drift is impossible.
- FairComparisonScore (gate-local): score scaled by the length fraction in BOTH directions. The review proved the matcher's 0.5 floor, mirrored into the gate, manufactured phantom margins on both sides ('Floyd' kept 90 at ratio exactly 0.5; a superstring tribute kept 90 against a floor-protected match). The floor is a recall device; the gate needs decision fairness.
- FuzzyMatcher.Score: single-candidate scoring without the best-match loop's >= 90 early exit. The gate now ranks ALL alternatives by fair score (raw >= 80 prefilter; length band inside Score), fixing the masking bug where 'Floyd' at index position 2 hid 'Pink Floyd' entirely.
- The alternative must keep fair >= AlternativeFullNameThreshold (80) to win (exemption-only partial-word hits cannot); IsRedundantShorterForm skips the comparison for shorter-form alternatives ('Miles' vs 'Miles Davis'), guarded on the match being a word-subset of the query so superstring tier-1 matches never skip.
- Same-review round fixes riding along (commit 7d4df1b): JF-428's creation-to-pin window closed (pin BEFORE the eviction sweep; cancelled gate wait unpins), and the JF-419.2 cancel-hatch-before-gate contract restored in PlaySong/PlayAlbum.

VERIFICATION
- TDD: Miles test red-first on old code; beatles-live phantom-margin and P!nk-floyd-with-Floyd masking authored from empirically probed scores (the review's original 'the beatles' scenario is unreachable through the inline search: tier 1 superstring-matches the tribute band; the reachable shape is 'beatles live' + an artist named 'Live' - documented in notes with the dump-test detour). ApplyFairLengthPenalty semantics pinned by unit test. Suite 2792 -> 2793 passed / 0 failed; Release build 0 warnings.
- Gates: /simplify 4 agents (wrapper deleted per 3 agents' convergence, named constants, named predicate, house split idiom; skips documented: deeper competitive-rule = follow-up, raw>=80 load-bearing, gate handler-local per JF-382 status quo); /code-review high round 2: 5 findings on this diff + unpushed work, ALL applied (2 gate arithmetic + masking bugs, pin window, hatch ordering, prose violations) or filed (JF-435 dry-run SMAPI leak, JF-436 bare-form competition; O(n) scan noted - cost class unchanged from the pre-existing FindBestMatchWithScore path, length-band prefilter included).
- Live verification: rides the next DLL deploy (the P!nk floyd behavior was live-verified in JF-420; the Floyd-masking and beatles-live shapes are library-dependent regressions, unit-proven).
- DoD 4/5/7/8 N/A (no session attrs, no HttpClient, no handler-shape change beyond the gate, no locale strings).
<!-- SECTION:FINAL_SUMMARY:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [x] #1 dotnet build passes with 0 errors
- [x] #2 dotnet test passes
- [x] #3 No new compiler warnings introduced
- [x] #4 Session attributes use proper DTOs not raw ValueTuples for serialization
- [x] #5 HttpClient instances are not shared across calls that modify BaseAddress
- [x] #6 NLU test fixtures updated if interaction model changed
- [x] #7 E2E test added for new intent or handler logic
- [x] #8 Locale response strings added to all 17 locales
- [x] #9 /simplify passed (no blocking cleanups remaining)
- [x] #10 /code-review high passed (no blocking findings remaining
- [x] #11 or findings applied/tracked)
<!-- DOD:END -->
