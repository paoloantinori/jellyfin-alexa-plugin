---
id: JF-589
title: >-
  Resume restarts from 0 when BOTH position stores are empty at offer time
  (2026-09-18 14:20 incident: restart cadence ate the write; queue files reset;
  context seed did not cover)
status: Done
assignee: []
created_date: '2026-09-18 12:31'
updated_date: '2026-09-18 13:47'
labels:
  - bug
  - resume
  - incident
  - reliability
dependencies: []
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
User report 2026-09-18 ~14:20 ('ho riaperto mia collezione, mi ha proposto di riprendere il podcast, ha funzionato ma e' ripartito dall'inizio'). VERIFIED STATE AT THE INCIDENT: (a) Jellyfin was stopped by systemd at 14:11:42 and the container RECREATED (fresh boot at 14:20:54, same auto-update class as the 04:09 incident - a SECOND recreation the same day; the boot at 14:20:54 is in the current container logs); (b) the user's playback of Ep. 1287 (7136e7c6, Morning) STARTED at 14:19:59 (UserData LastPlayedDate, PlayCount=1) - i.e., their resume-yes launched ONE MINUTE before the 14:20:54 boot killed the session; (c) BOTH position stores are EMPTY for the incident item: server UserData PlaybackPositionTicks=0 (the JF-581 write-loss class: the stop never landed, the restart ate it) AND the persisted device-queue store shows NO ItemPositionState entries for any Morning episode (8 queue files touched at 14:11-14:19 all with lastPlayed=none/items=0/posState=0 - whether wiped or freshly-created is NOT determinable: the previous container's logs died with the recreation, so the user's actual LaunchRequest offer chain (the resume_state offset offered) is not recoverable). NET: the offer had nothing to seed from - both fallback legs (UserData -> ItemPositionState) were empty, so offset 0 was the only possible answer; the JF-581 defense could not fire because its INPUT was already lost. OPEN QUESTIONS: (1) what emptied/reset the device queue files at 14:11 (mass empty persists across 8 devices at the stop - a shutdown flush of in-memory state that had already lost the disk content? or fresh files?); (2) why the LaunchRequest AudioPlayer-context seed (device-derived offset, survives restarts by platform contract) did not provide the position - candidates: the context token/offset were stale or absent after the 14:11 session wipe (the JF-567 finding 7 shape), or the offer came from the device-last-played seed with both stores empty; (3) the restart cadence itself (04:09 + 14:11 + 14:20 same day) - the second stop at 14:11:42 was systemd-initiated (journal-verified), trigger unknown (NOT the 04:06 timer slot). NEXT CAPTURE: the CURRENT container logs are fresh - ask the user to listen via the skill, PAUSE (the pause writes the position to BOTH stores - verify the write lands with a log check), then re-open and resume; if the pause-write lands and the resume still starts at 0, the bug is in the offer/read path and the fresh logs will show it end to end. Related: JF-581 (the write-loss defense), JF-588 (the session-miss self-heal; different failure leg), JF-567 finding 7 (the pre-tail token guard shape).
<!-- SECTION:DESCRIPTION:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [ ] #1 dotnet build passes with 0 errors
- [ ] #2 dotnet test passes
- [ ] #3 No new compiler warnings introduced
- [ ] #4 Session attributes use proper DTOs not raw ValueTuples for serialization
- [ ] #5 HttpClient instances are not shared across calls that modify BaseAddress
- [ ] #6 NLU test fixtures updated if interaction model changed
- [ ] #7 E2E test added for new intent or handler logic
- [ ] #8 Locale response strings added to all 17 locales
- [ ] #9 /simplify passed (no blocking cleanups remaining)
- [ ] #10 /code-review high passed (no blocking findings remaining or findings applied/tracked)
<!-- DOD:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
SECOND CAPTURE (2026-09-18 14:48-14:51, log-verified end to end after the user reported 'ho fatto andare il play per piu' di un minuto prima di stoppare') - THE DECISIVE EVIDENCE: (1) 14:48:52 PlayNextEpisodeIntent('riproduci l'ultimo episodio di morning', slot ER_SUCCESS_MATCH id=latest) - the request CONTEXT already carried AudioPlayer offset=27599ms token=7136e7c6 playerActivity=STOPPED (residual from an earlier session of the same item); (2) the handler launched a FRESH VideoApp.Launch from 0 (server stores empty per the first incident; the recency core had no resume input) via the STATIC /Videos route ('video codec unknown; keeping the static stream'); (3) the user played 81 SECONDS of wall time (14:48:52 to the 14:50:13 stop) but the stop's context reported offset=27599ms - EXACTLY the pre-launch value, i.e. the position counter NEVER MOVED: VideoApp playback updates neither the AudioPlayer context nor emits any event, so the position of that entire listen was invisible; (4) 14:50:24 reopen -> the offer seeded from the STALE 27599 (the only position the platform exposes); 14:50:38 yes -> AudioPlayer.Play offset=27599 (the tracking path) -> PlaybackStarted 14:50:48 -> user paused 14:50:55 at offset=32221ms = 27599 + 4622ms REAL advancement (the resume mechanism itself is proven correct: the device counter continued from the resume point); 14:50:57 the stop wrote 33413ms to BOTH stores (ItemPositionState + the JF-581 direct UserData write fired: 'did not land via the session report; wrote it directly'). USER EXPERIENCE ACCOUNTED FOR: the fresh ask started from 0 (nothing to resume from) and the subsequent resume landed at 27.6s - BEHIND the ~73s they had just listened - because the VideoApp listen's position was never tracked. ROOT CAUSE (sharper than the first capture): podcast episodes are Episode items (video-kind), so on the Echo Show they route through VideoApp, which is POSITION-BLIND (no events, no context updates, the documented platform gap); the AudioPlayer path tracks correctly. This is the Show-side twin of JF-586 (episodes on Dots). FIX DIRECTIONS: (a) PREFERRED: audio-content episodes (.strm podcasts, effectively audio-only) should route AudioPlayer even on VideoApp-capable devices - either a per-series/per-library podcast flag or a content-shape probe (the JF-586 predicate inverted: Episode + audio-shaped -> AudioPlayer by design, not degrade); the seek-bar trade-off is acceptable for podcasts and position tracking is the higher good; (b) MINIMUM: the fresh-episode ask (recency/NextUp cores) should seed the launch resume from the context offset when context.AudioPlayer.Token == the resolved episode id and offset > 0 (the same-item re-ask shape this incident exhibited); (c) the stale-context guard family (JF-567 finding 7) applies to the offer seed: a context position whose token matches the device last-played but predates a VideoApp session of the same item is the best available signal - keep using it, but the real fix is (a). VERIFICATION PROTOCOL FOR THE FIX: repeat this exact sequence (fresh ask >1 min, stop, reopen, resume) - the fix is proven when the resume lands at the ~1min+ point, not at a stale pre-launch value.

IMPLEMENTED 2026-09-18 (both directions, uncommitted for the orchestrator's gates). DIRECTION (a) SHAPING: the audio-shaped signal is candidate 2, CONTENT-TRUTH: the episode's media streams positively prove audio-only (at least one audio stream and NO video stream) via `IsAudioOnlyEpisode` in PlaybackLaunchBuilder - a GetMediaStreams probe of the same kind the codec routing already performs (zero new I/O class; one extra in-process read on the Episode capable path). Probe limits documented in the method doc: zero streams and no-audio-stream both count as UNKNOWN and keep VideoApp, and a thrown probe fails OPEN (VideoApp on capable devices) - the audio route fires only on positive audio-only evidence. REJECTED candidate 1 (.strm + podcast-ish library - fragile, container/library heuristics) and candidate 3 (config flag - pushes per-item truth onto the user). ROUTING CHANGE: BuildEpisodeLaunchResponseAsync now degrades when `item is Episode && (screenless || (capable && IsAudioOnlyEpisode(item)))`; the JF-586 degrade body was extracted verbatim into BuildEpisodeAudioDegrade so the two conditions share one clamp/ResolveAudioLaunchSource/announce-ride chain. Real TV episodes (a video stream present) keep the VideoApp path unchanged (the existing capable-device pin test stays green as the no-regression proof); the trade-off, documented on the method: audio-only episodes LOSE the VideoApp seek bar they technically could have had - position tracking is the higher good (the 2026-09-18 capture: 81s of VideoApp listening never moved the counter, so the next resume landed BEHIND the listen). Note the interaction with JF-498 routing: today 'video codec unknown; keeping the static stream' sent .strm episodes to VideoApp-static; with the audio-only signal the same items route through ResolveAudioLaunchSource (static /Audio for decodable audio, the episode audio HLS transcode for eac3), reusing the JF-586 degrade machinery unchanged. DIRECTION (b) SEED GUARD: in TvNextUpService.LaunchEpisodeAsync, after ResolveResumeTicks reads BOTH stores, when resumeTicks == 0 AND userData?.Played != true AND context.AudioPlayer carries OffsetInMilliseconds > 0 whose Token parses via StreamTokenCodec.TryGetItemId to the SAME episode id, the context offset (ms -> ticks) becomes the resume input; the existing ClampResumeTicksToRuntime and the resumeDelivered announce gate apply downstream unchanged, so a stale-context offset beyond the runtime still fails closed to a fresh start. Played wins too: a completed listen means the re-ask wants a fresh start, not the stopped tail. FILES: Jellyfin.Plugin.AlexaSkill/Alexa/Util/PlaybackLaunchBuilder.cs (probe + route + shared degrade body), Jellyfin.Plugin.AlexaSkill/Alexa/Handler/TvNextUpService.cs (seed). TESTS: VideoAppCapabilityGateTests added BuildEpisodeLaunchResponse_AudioOnlyEpisodeOnCapableDevice_RoutesAudioPlayer (the incident item shape) and BuildEpisodeLaunchResponse_ProbeThrowsOnCapableDevice_KeepsVideoApp (fail-open pin; the pre-existing capable-device VideoApp test is the video no-regression pin); TvNextUpServiceTests added the three seed arms (stores empty + matching token -> context offset on the directive; stored position wins; foreign token -> no seed). VERIFY: dotnet test 4074/4074 PASSED on net9.0 AND net10.0; dotnet build -c Release 0 warnings. No interaction-model or locale changes. VERIFICATION PROTOCOL still open on-device: fresh ask >1 min, stop, reopen, resume must land at the listened point.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
DONE (commit fa8447cc). AUDIO-SHAPED SIGNAL (candidate 2, content-truth): PlaybackLaunchBuilder gains IsAudioOnlyEpisode (media streams: >=1 audio + NO video stream, fail-open on absence/throw of the probe; zero extra I/O on paths that already probe) and BuildEpisodeLaunchResponseAsync now degrades AUDIO-ONLY EPISODES to the AudioPlayer route EVEN ON VIDEO-APP-CAPABLE DEVICES (the Echo Show), while real TV episodes and probe-fail-open shapes keep VideoApp unchanged (byte-identical capable path pinned). The JF-586 degrade body was extracted verbatim into BuildEpisodeAudioDegrade shared by both conditions (verified line-for-line against HEAD~1). The trade-off (no seek bar on audio-only episodes) is documented on the method doc with the JF-589 rationale (position tracking > seek bar for audio content). SAME-ITEM RE-ASK SEED (direction b): TvNextUpService.LaunchEpisodeAsync seeds the resume from the residual context offset when the stores are empty and the context token parses to the SAME episode id (StreamTokenCodec idiom), the JF-565 clamp applied downstream; the degrade route delivers it as ?start= on the transcode or the directive offset on the static route. ROOT CAUSE of the incident sharpened with the second capture: VideoApp playback is POSITION-BLIND (no events, no context updates), so every VideoApp listen of a podcast episode loses its position entirely; the fresh ask then had nothing to seed from and the resume landed at a stale pre-launch value (27599) behind the ~73s just listened. The static-route VideoApp branch for unknown codecs now also routes .strm episodes through ResolveAudioLaunchSource (static /Audio for decodable, audio HLS transcode for eac3), so these items are position-tracked going forward. GATES: /simplify 4-angle + adversarial combined pass - no blockers; extraction verified verbatim; units verified no double conversion; probe fail-open verified; the VideoApp-no-false-seed property verified (VideoApp sessions never set context.AudioPlayer.Token so the seed cannot fire from VideoApp listens); two trivial doc nits applied. Tests: 5 new (audio-only episode on capable device -> AudioPlayer.Play /Audio/ route; video episode on capable device -> VideoApp unchanged; probe-throw fail-open; context-offset seed on stores-empty; stored-position-wins + different-token-no-seed guards). Suite 4074/4074 both TFMs, Release 0 warnings. LEFT OPEN: the end-to-end on-device protocol from the task notes (fresh ask >1 min, stop, reopen, resume must land at the listened point) - for the user to verify on the Show/Dot after this deploy; un-scanned items (empty streams) stay on the old VideoApp path by design (fail-open) until Jellyfin probes the file.
<!-- SECTION:FINAL_SUMMARY:END -->
