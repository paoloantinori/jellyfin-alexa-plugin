---
id: JF-419.2
title: >-
  Warming gate covers only 4 of 10 artist-search entry points: move to a single
  choke point
status: Done
assignee:
  - zai
created_date: '2026-08-31 15:01'
updated_date: '2026-09-01 04:12'
labels:
  - code-review
  - architecture
dependencies: []
references:
  - 'Jellyfin.Plugin.AlexaSkill/Alexa/Handler/BaseHandler.cs:1492'
  - Jellyfin.Plugin.AlexaSkill/Alexa/Util/ArtistSearch.cs
parent_task_id: JF-419
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Code-review finding (2026-08-31, high effort) on the JF-419 implementation. BaseHandler.cs:1492 copied per handler.

DEFECT: the warming gate is copy-pasted into only 4 handlers while 6 other artist-search entry points still hit the slow cold-index DB path: AddToQueueIntent, MediaInfoIntent, PlayMoodMusic (via BaseHandler.TryEntityFallbackAsync), PlayNextIntent, QueryArtistLibraryIntent, SearchMediaIntent. During the cold-start window the user gets the friendly warming message for "suona i pink floyd" but an 8s+ stall and on-device INVALID_RESPONSE for "quali album ha pink floyd", "aggiungi pink floyd alla coda", or a PlayMoodMusic miss routing through TryEntityFallbackAsync. Same live-incident class JF-419 was built from, still reachable.

FIX SHAPE: move the gate to one choke point instead of per-handler copies. Natural candidates: ArtistSearch.SearchAsync (all artist searches funnel there) or the request pipeline. Watch the two documented copies of the search chain (ArtistSearch.SearchAsync + inline PlayArtistSongs Thorough/Fast mode, JF-382): the choke point must cover BOTH, including the Fast-mode DB path.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 A single gate location (one method) covers all 10 artist-search entry points: the 4 already gated (PlayArtistSongs, PlaySong, PlayAlbum, FindSong) plus AddToQueue, MediaInfo, PlayMoodMusic (via TryEntityFallbackAsync), PlayNext, QueryArtistLibrary, SearchMedia
- [x] #2 The per-handler gate copies in BaseHandler-using handlers are removed (grep IsArtistIndexWarm finds one call site)
- [x] #3 Unit test per newly-gated entry point: warming index returns the warming response
- [x] #4 No behavior change when the index is ready
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
## Implementation Plan (JF-419.2)

**Approach**: the warming gate becomes a thrown exception at the single funnel (ArtistSearch) + one translator in the request pipeline. Verified on the code: all 10 entry points call ArtistSearch.SearchAsync (the task description's own audit) except PlayArtistSongs' inline JF-382 copy; no caller wraps SearchAsync in catch(Exception) except MediaInfo's enrichment path, where graceful degradation is the CORRECT policy (artist-info text is optional enrichment of a now-playing answer).

**Changes:**
1. NEW Alexa/Exceptions/SkillWarmingUpException.cs (sealed, no state needed).
2. ArtistSearch: `EnsureIndexReady(IArtistIndex?)` containing THE single IsReady check (throws SkillWarmingUpException). Called at the top of SearchAsync and at PlayArtistSongs' inline search entry (the JF-382 second copy - the Fast-mode DB path included).
3. RequestPipeline.ExecuteAsync: wrap HandleRequestAsync in try/catch(SkillWarmingUpException) -> Tell(SkillWarmingUp, requestContext.Locale), info-level log. One translation point for all intents.
4. Remove the 4 per-handler gate copies (PlayArtistSongs:140, PlaySong:144, PlayAlbum:99, FindSong:155) and delete BaseHandler.IsArtistIndexWarming + BuildSkillWarmingUpResponse (dead). PlayArtistSongs keeps a one-line EnsureIndexReady call at the same position (it guards the INLINE search copy). Behavior improvement accepted: PlaySong/FindSong/PlayAlbum artist-LESS paths now proceed during warming instead of being refused (partially addresses JF-419.3's inverse-window complaint); their artist searches still refuse via the choke point.
5. MediaInfo enrichment: existing catch(Exception) swallows -> graceful degrade (documented, no code change).

**Tests (TDD):**
1. ArtistSearchTests: SearchAsync with warming index throws SkillWarmingUpException (choke point).
2. SkillWarmingUpTests rewritten: the 2 existing handler tests become Assert.Throws; NEW pipeline test (stub handler throws -> ExecuteAsync returns Tell containing the locale string) proving translation; per-entry-point tests for the newly gated: AddToQueue, QueryArtistLibrary, SearchMedia, PlayNext (Assert.Throws from HandleAsync), MediaInfo (asserts graceful degrade: no throw, response without artist enrichment). PlayMoodMusic's TryEntityFallbackAsync path covered structurally via BaseHandler:2531 -> same choke point (no try around it; noted in task).
3. AC #2 interpretation: grep IsArtistIndexWarming -> 0 hits (method deleted; the single check lives in ArtistSearch.EnsureIndexReady).

**Verification**: full dotnet test, /simplify + /code-review gates, commit with markers. No model/locale changes (SkillWarmingUp string unchanged).
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
2026-08-31 /simplify (4 agents) applied: PlayArtistSongs guard moved AFTER slot validation but BEFORE the progressive response (reconciled Efficiency-vs-Altitude disagreement: DidNotCatch wins for slot-less utterances during warming, and no 'searching' announcement precedes a refusal); exception doc trimmed to house norm (3 lines) with index-agnostic message (prerequisite for JF-419.3 reuse); pipeline log fields dropped (BeginScope already carries intent+corr); PlayArtistSongs comment to one line; test lambda param renamed artistIndex; both hand-rolled warming fakes replaced by Mock.Of<IArtistIndex>(i => i.IsReady == false).

Skips documented: MediaInfo explicit catch(SkillWarmingUpException) before catch(Exception) (the broad catch already degrades; an explicit catch would only document intent - noted here instead); guard-before-progressive-response nuance per altitude agent.

Altitude agent verdict: correctly scoped across the board. Caller matrix verified: 10 refuse sites + MediaInfo degrade; pipeline placement BETTER than controller (covers the FindSong-session route, the handler loop, AND SimulatorController while keeping response interceptors in play). Follow-up notes: (a) PlayMoodMusic mood-miss with empty genre results now says warming instead of NotFoundMood in the rare empty-genre path - accepted for the self-healing window; (b) JF-419.3 should REUSE SkillWarmingUpException + the same pipeline catch (one type, two throw sites); the index-agnostic message applied in this task is the prerequisite; consider an IndexName property for the log.

Full suite 2780/2780 after fixes; grep IsArtistIndexWarming/BuildSkillWarmingUpResponse = 0 hits (AC #2). /code-review high running.

2026-09-01 DEPLOYED + VERIFIED (DLL b79870d9, AlexaSkill_0.12.0.0, config 1 user survived, active-DLL MD5 match, boot log clean: index loaded 1149 artists, interceptor chain running per ResponseBodyLogging). Live matrix: Soul Coughing auto-plays (JF-420.1 intact through the new entry guards), xyzzyfoo clean not-found, QueryArtistLibrary 'pink floyd' answers normally. Warming window NOT catchable via the simulator endpoint (2 attempts: the endpoint answers only after the index load completes, so the externally observable window is closed; the mechanism is unit-proven through real handler + real pipeline).
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
JF-419.2: the warming gate is now a single two-layer mechanism covering every artist-search entry point; no per-handler logic copies remain.

WHAT CHANGED (commit e14b0cc, 19 files)
- Layer 2 choke point: ArtistSearch.EnsureIndexReady (THE single IsReady check) throws SkillWarmingUpException at SearchAsync entry, covering all 13 ArtistSearch callers incl. BaseHandler.TryEntityFallbackAsync and future handlers. New Alexa/Exceptions/SkillWarmingUpException.cs (index-agnostic message: prerequisite for JF-419.3 reuse).
- Layer 1 entry guards: one-line EnsureIndexReady calls in 9 handlers whose primary path queries the same cold DB (PlaySong/PlayAlbum/FindSong/SearchMedia/PlayMoodMusic/AddToQueue/PlayNext at entry; PlayArtistSongs for its inline JF-382 copy; QueryArtistLibrary before its announcement). Review round 2 proved the choke-point-only version RE-EXPOSED the title/album/keyword paths (the DB is cold, not just the artist index): those guards are load-bearing, not belt-and-braces.
- Pipeline translation: RequestPipeline catches the throw and builds the one warming Tell for all 3 dispatch paths (controller FindSong-session route, handler loop, SimulatorController). SkipColdLibraryWork flag keeps metrics/logging interceptors running while skipping DynamicEntities' cold DB work (round 3: early-return made warming requests invisible to metrics exactly in the incident window).
- MediaInfo: explicit catch -> degrade (skip artist info) instead of a misleading per-request warning. FindSong: guard placed AFTER the cancel escape hatch (open flows still cancel during warming).
- Deleted: BaseHandler.IsArtistIndexWarming + BuildSkillWarmingUpResponse, the dead stacked GetSearchResponseMode doc.

REVIEW PROCESS (this task earned its gates twice)
- /simplify 4 agents: guard placement reconciled (Efficiency vs Altitude disagreement resolved by reading the code), doc trims, Moq one-liners, DRY test helper.
- /code-review high (retried run): 10 findings, ALL applied - including 3 CONFIRMED regressions of my own first version (title/album/keywords paths re-exposed), the interceptor-chain cold-query ride-along on the refusal, announcement-then-refusal ordering in 6 handlers, the stub-only pipeline test that could not see a broad catch, and conventions nits. First-run angles A-H (which reviewed mid-edit snapshots) contributed: PlayMoodMusic missed in the sweep (A2/B1), conditional-vs-unconditional gate inconsistency (A1/B2), metrics bypass (A4/B3), cancel-hatch ordering (A3), observability of the refusal log (C4), MediaInfo warning spam (F1), comment/test hygiene (E1-E6).
- Residuals documented, not fixed (out of scope): unit tests bypass CanHandle/controller routing (repo-known gap, memory note exists); SearchMedia/FindSong refuse coarse during warming where a degraded answer was theoretically possible (JF-419.3 granularizes per-index); JF-419.3 should reuse SkillWarmingUpException + the same pipeline catch (one type, two throw sites) and mind the AudioPlayer-event Tell restriction if a throw site ever lands in an event handler.

VERIFICATION
- Full suite 2780 passed / 0 failed (TDD red-first for the choke point, entry gates, and pipeline translation; PlayArtistSongs contract test updated to throw + DB-never-queried). Release build 0 warnings (TreatWarningsAsErrors).
- Live verification deferred to the next DLL deploy (bundle with JF-428; the warming window itself is post-restart, so the live check is 'first request after deploy answers warming Tell, not INVALID_RESPONSE').
- DoD 4/5/8 N/A (no session attrs, no HttpClient, no locale strings - SkillWarmingUp key already in all 17 locales).
<!-- SECTION:FINAL_SUMMARY:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [x] #1 dotnet build passes with 0 errors
- [x] #2 dotnet test passes
- [x] #3 No new compiler warnings introduced
- [x] #4 Session attributes use proper DTOs not raw ValueTuples for serialization
- [x] #5 HttpClient instances are not shared across calls that modify BaseAddress
- [x] #6 NLU test fixtures updated if interaction model changed
- [x] #7 E2E test added for new intent or handler logic
- [x] #8 Locale response strings added to all 17 locales
- [x] #9 /simplify passed (no blocking cleanups remaining)
- [x] #10 /code-review high passed (no blocking findings remaining
- [x] #11 or findings applied/tracked)
<!-- DOD:END -->
