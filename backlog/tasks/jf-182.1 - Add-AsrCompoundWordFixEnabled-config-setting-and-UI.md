---
id: JF-182.1
title: Add AsrCompoundWordFixEnabled config setting and UI
status: Done
assignee: []
created_date: '2026-05-18 18:36'
updated_date: '2026-05-19 17:03'
labels:
  - config
  - feature-flag
dependencies: []
parent_task_id: JF-182
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Add the `AsrCompoundWordFixEnabled` boolean property to `PluginConfiguration.cs` with default `true`. Add the checkbox to the Feature Flags section in `config.html` with label "ASR Compound Word Fix" and description "When Alexa splits compound words (e.g. 'lazybones' → 'lazy bones'), retry search with joined variants. Disable if search is too slow on misses."

Wire the new property into the config.html JS load/save arrays alongside the other feature flags.

Write unit tests verifying the default is `true` and the property round-trips through config serialization.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 Property AsrCompoundWordFixEnabled exists on PluginConfiguration with default true
- [ ] #2 Config UI checkbox appears in Feature Flags accordion with label and description
- [ ] #3 JS load/save arrays include the new property
- [ ] #4 Unit test confirms default value is true
- [ ] #5 Unit test confirms XML serialization round-trip
<!-- AC:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Added `AsrCompoundWordFixEnabled` boolean config property (default true), wired into config.html Feature Flags section (checkbox + JS load/save/generalConfig), added default-value assertions to shared `PluginConfigurationFeatureFlagTests`, and added XML serialization round-trip test. Also wired `ResumeAnnounceTitle` into the config UI (property existed but wasn't in HTML/JS).
<!-- SECTION:FINAL_SUMMARY:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [ ] #1 dotnet build passes with 0 errors
- [ ] #2 dotnet test passes
- [ ] #3 No new compiler warnings introduced
- [ ] #4 Session attributes use proper DTOs not raw ValueTuples for serialization
- [ ] #5 HttpClient instances are not shared across calls that modify BaseAddress
- [ ] #6 NLU test fixtures updated if interaction model changed
- [ ] #7 E2E test added for new intent or handler logic
- [ ] #8 Locale response strings added to all 12 locales
<!-- DOD:END -->
