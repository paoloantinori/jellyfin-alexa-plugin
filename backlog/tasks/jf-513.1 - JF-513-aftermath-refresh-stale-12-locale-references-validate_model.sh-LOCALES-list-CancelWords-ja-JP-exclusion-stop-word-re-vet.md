---
id: JF-513.1
title: >-
  JF-513 aftermath: refresh stale 12-locale references (validate_model.sh
  LOCALES list; CancelWords ja-JP exclusion + stop-word re-vet)
status: In Progress
assignee: []
created_date: '2026-09-11 20:19'
updated_date: '2026-09-12 00:12'
labels: []
dependencies: []
references:
  - scripts/validate_model.sh
  - Jellyfin.Plugin.AlexaSkill/Alexa/Util/CancelWords.cs
  - JF-513
parent_task_id: JF-513
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Filed during the JF-513 implementation session (review findings outside that task's two-file scope). Two references in the repo still describe the pre-JF-513 12-locale state and go stale the moment JF-513 lands:

1. scripts/validate_model.sh line 33: the 'all' mode hardcodes LOCALES="de-DE en-AU en-CA en-GB en-IN en-US es-ES es-MX es-US fr-CA fr-FR it-IT" (12 entries). After JF-513 the skill has 17 locales; the list must include ar-SA, hi-IN, ja-JP, nl-NL, pt-BR, or be derived from the model files (ls Jellyfin.Plugin.AlexaSkill/Alexa/InteractionModel/model_*.json) instead of hardcoded.

2. Jellyfin.Plugin.AlexaSkill/Alexa/Util/CancelWords.cs ~lines 67-71: the exclusion note says 'ja-JP has NO deployed model on the live skill (profile-nlu and get-interaction-model both return HTTP 400; ja-JP is absent from manifest.json's 12 locales), so probes cannot vet any Japanese word... when a ja-JP model is deployed, re-vet (candidates: とめて/ストップ) before adding an entry.' JF-513 declares ja-JP in the manifest, so after the JF-513 redeploy the model builds and this re-vet trigger FIRES: probe the Japanese stop/cancel candidates via profile-nlu (ask smapi profile-nlu, locale ja-JP), add a ja-JP entry to the locale word sets if any route to AMAZON.StopIntent/AMAZON.CancelIntent, and update the comment to describe the new state (or re-document the exclusion with fresh probe evidence if nothing routes).

Context: these were found by the four-angle /simplify review of the JF-513 diff; the JF-513 worker was scoped to manifest.json + ManifestSkillTests.cs only. Item 2's probe half depends on the JF-513 redeploy having run (get-skill-status showing ja-JP build SUCCEEDED).

Item 1 RESOLVED in the JF-513 /simplify pass (2026-09-11): validate_model.sh now derives LOCALES from the committed model files (`ls .../model_*.json | sed`), so it can never drift again. Items 3 and 4 joined from the same review's altitude pass (same stale-locale-reference class, pre-existing):

3. Jellyfin.Plugin.AlexaSkill.Tests/Unit/LocaleStringsTests.cs ~line 15: `AllLocales` hardcodes the 17-locale list as a static MemberData array with no exhaustiveness guard; locale #18 requires a manual edit here and nothing fails when forgotten, so a new locale silently skips the JF-487 SSML-separator and VideoRequiresScreen invariants. Fix shape: derive from Util.GetLocalInteractionModels() like the new Manifest_DeclaresEveryInteractionModelLocale test does.

4. scripts/generate_voice_reference.py ~line 34: `LOCALE_ORDER` errors on listed-but-missing locales but silently ignores EXTRA ones (its own ~line 443 comment admits the emitter walks LOCALE_ORDER and --check stays green), so VOICE_COMMANDS_BY_LOCALE.md would silently omit locale #18. Fix shape: derive the full set from the model files and warn/error on LOCALE_ORDER divergence.
   CORRECTION (code-review verification, 2026-09-11): the premise above is STALE. generate_voice_reference.py:445-450 already hard-fails bidirectionally (sys.exit on locales not in LOCALE_ORDER; empirically probed by adding a model file). The only residual obligation is extending LOCALE_ORDER when locale #18 lands, and the existing guard enforces that loudly. No fix needed; item kept as the record of the verification.

5. Jellyfin.Plugin.AlexaSkill/Alexa/Catalog/CatalogManager.cs ~line 751: the poll-budget comment sizes the 90-iteration (~150-170s) wait on "a full 12-locale sync" (48 serialized SMAPI builds). JF-513 makes it 17 locales / 68 builds, so the sizing rationale is stale and the budget can regress to the documented TIMEOUT-on-every-locale failure (the 2026-09-06 canary-never-fires incident). Re-derive the budget from the live locale count (or scale by it) and update the comment.

6. Test-project locale enumeration: ~6 per-file copies of the enumerate-locales-from-embedded-models iterator (InteractionModelTests, MusicianSlotTypeTests x2, SlotMappingsTests, SkillInteractionModelMoodInjectionTests, ItalianNumberWordsTests, now ManifestSkillTests). Add one shared public TestLocales.AllLocales() helper (TestHelpers.cs pattern) returning bare locale strings and migrate the discovery-based callers; keep the hardcoded MemberData arrays (LocaleStringsTests, ResponseStringsTests) as the item-3 exhaustiveness gap.
<!-- SECTION:DESCRIPTION:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [ ] #1 dotnet build passes with 0 errors
- [ ] #2 dotnet test passes
- [ ] #3 No new compiler warnings introduced
- [ ] #4 Session attributes use proper DTOs not raw ValueTuples for serialization
- [ ] #5 HttpClient instances are not shared across calls that modify BaseAddress
- [ ] #6 NLU test fixtures updated if interaction model changed
- [ ] #7 E2E test added for new intent or handler logic
- [ ] #8 Locale response strings added to all 17 locales
- [ ] #9 /simplify passed (no blocking cleanups remaining)
- [ ] #10 /code-review high passed (no blocking findings remaining or findings applied/tracked)
<!-- DOD:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
2026-09-12 /code-review high pass (origin/main..HEAD) appended two untracked items to this task's stale-locale-reference class:

5. Jellyfin.Plugin.AlexaSkill/Alexa/Catalog/CatalogManager.cs ~lines 751-756: the poll-budget rationale comment in WaitForModelBuildOutcomeViaSkillStatusAsync sizes the 90-iteration (~150-170s) budget on live 12-locale evidence ('a full 12-locale sync queues FOUR serialized SMAPI builds per locale... settles at ~80s+ of queue', live 2026-09-06). After the JF-513 redeploy the queue holds 68 builds (17 locales x 4), per-locale settle time grows, and the budget can TIMEOUT on every locale again (the exact canary-never-fires failure the comment documents). Update the comment and re-derive the iteration count; watch the first post-redeploy catalog sync for TIMEOUTs.

6. Test-project locale enumeration is copy-pasted: Util.GetLocalInteractionModels() is iterated independently in ManifestSkillTests.cs:53 (new in JF-513), InteractionModelTests.cs:25, MusicianSlotTypeTests.cs:37+:112, SlotMappingsTests.cs:108, SkillInteractionModelMoodInjectionTests.cs:17, ItalianNumberWordsTests.cs:52, DispatchRoutingTests.cs:351, ModelDeploymentManagerTests.cs:599, plus LocaleStringsTests.cs:15 hardcoded AllLocales (item 3). A shared TestLocales.AllLocales() helper would single-source the pattern; locale #18 currently touches ~10 files with no exhaustiveness guard outside the manifest test.

Item 2 RESOLVED 2026-09-12 (night run): the JF-513 redeploy built ja-JP, and the re-vet probes (correct extractor) routed とめて/止めて/止まって/ストップ to StopIntent and やめて to CancelIntent; JapaneseWords set added with the evidence table rows and the test moved into the shared theories. NOTE the incident recorded in CancelWords.cs: the first probe pass that night used a broken extractor (result.intent instead of selectedIntent) and briefly re-documented the exclusion on all-NO_SELECTION false evidence; corrected within the hour.

7. VOICE_COMMANDS.md row-vs-model count guard (from the JF-399 /simplify altitude pass): the hand-maintained utterance table is verified today only by the PlayAlbum-specific warning check #10 in validate_interaction_models.py; every other intent's rows can go stale silently (the known PR #15 / JF-459 failure mode, now caught only by eyeball). Add a warning-level check in the same shape as the Phase 5 template-drift walk: per locale x intent, compare the row's sample count (and ideally the sample strings) against the generated model. Same file, same severity convention (advisory CI job).

RESOLUTIONS (night run, 2026-09-12):
- Items 3 + 6 RESOLVED: TestLocales.cs at the test project root (PluginCollection convention) owns the roster (Models/AllLocales/LocaleRows/LocalesWithResourcePaths/ResourcePath/NonEnglishLocales/ResponseStringLocales); 10 files migrated. BONUS from the code-review pass: ResponseStringsTests' non-English list was STALE at 9 entries - es-MX/es-US/fr-CA silently skipped the Welcome-differs assertion and now cover all 12; its two hardcoded theory rosters (17 InlineData rows + the en-family) also derived. Suite 3635/3635.
- Item 5 RESOLVED with live evidence: the first 17-locale catalog sync post-JF-513 completed 17/17 locales at 79-132s each with ZERO model-build TIMEOUTs (watched live 02:08-02:39 container time), so the 90-iteration budget is sized on the serialized-queue depth rather than the locale count and held at 68 queued builds. Comment updated to the measured reality (no code change: the empirical risk retired).
- NEW WATCH ITEM (ar-SA, from the same sync): the catalog wiring's model PUT for ar-SA ended LANGUAGE_MODEL_FULL_BUILD FAILED with NO error detail (the only locale of 17; its sync leg itself completed in 106s with no logged error). Restored immediately via the embedded-model rebuild (canary OK, 59 intents / 300 samples live-matching). IF the next 12h sync re-fails ar-SA the same way, it is a systematic catalog-wiring bug to file as its own task (first candidate: an Arabic catalog value colliding with a builder rule); if it passes, mark transient.
- Item 7 RESOLVED (night run, 2026-09-12): Phase 6 warning lint lint_voice_commands_rows in validate_interaction_models.py (stale row utterances, missing intent rows, unmappable titles, retired-intent rows, unparseable rows/headings - all five shapes mechanically exercised; partial rows documented-not-warned; the file header now honestly says "selected utterances" and points at the generated complete list). The 91 missing rows were GENERATED into VOICE_COMMANDS.md (PlayBook/ShowMore/FindSong/FindSongByArtist/ShufflePlay everywhere plus scattered LoopSongOn/SkipForwardBack/JumpToPosition; verified byte-faithful, dense-table format preserved). BONUS from the review: docs/VOICE_COMMANDS_BY_LOCALE.md had drifted since 2026-09-11 (JF-541 phase-1 model change) - regenerated, and its generate_voice_reference.py --check is now wired into BOTH ci.yml (own step, advisory job) and the blocking release-build.yml.
- Item 8 (filed from the item-7 review, strategic): VOICE_COMMANDS.md has NO committed writer - the 91 rows came from a throwaway stdin script, and every future model edit re-opens the 17-section hand-sync treadmill with only a warning as the net. Fix shape: a curated-density emit mode on generate_voice_reference.py (its GROUPS table already maps every intent to a label and walks all 17 locales) that emits VOICE_COMMANDS.md too; the curated partial rows (120 of 779) are the open design question (preserve-as-curated vs emit-complete). Until then the Phase 6 lint is the guard.
- Task status: all items resolved or documented (1 resolved, 2 resolved, 3+6 resolved, 4 verified-no-action, 5 resolved-with-live-evidence + ar-SA watch, 7 resolved, 8 filed); the ar-SA catalog-wiring watch item is the only live follow-up (re-check on the next 12h sync).
<!-- SECTION:NOTES:END -->
