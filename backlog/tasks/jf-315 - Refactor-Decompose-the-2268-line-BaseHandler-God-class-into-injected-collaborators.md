---
id: JF-315
title: >-
  Refactor: Decompose the 2268-line BaseHandler God class into injected
  collaborators
status: To Do
assignee: []
created_date: '2026-07-12 14:58'
updated_date: '2026-09-15 17:44'
labels:
  - refactor
  - maintainability
  - tech-debt
milestone: m-7
dependencies: []
references:
  - Jellyfin.Plugin.AlexaSkill/Alexa/Handler/BaseHandler.cs
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
`Alexa/Handler/BaseHandler.cs` is 2268 lines with 60+ protected members spanning response building, fuzzy search, ASR fallback, APL directives, playback progress reporting, radio, playlists, resume-index math, XML/SSML escaping, and URL building. All 61 handlers inherit ALL of it. This is the single biggest maintainability risk in the codebase (architecture review 2026-07-12): any change ripples to every handler and the class is impossible to reason about in isolation.

This is strategic debt, not a bug — scope it as a deliberate, incremental refactor. Extract cohesive collaborators (e.g. ResponseFactory, SearchService, ProgressReporter, UrlBuilder, SsmlBuilder) and inject them, migrating handlers in batches while keeping the test suite green. Do NOT attempt in one big-bang change. Consider doing this as a parent task with per-collaborator subtasks. The large existing test suite (159 test files, ~2360+ cases) is the safety net that makes this feasible.

Related: BaseHandler currently forces the singleton stateless-by-luck constraint on every handler (see the concurrency milestone) — extracting state-free collaborators reduces that surface too.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 A target decomposition is documented (which responsibilities move to which collaborator) before code changes
- [ ] #2 At least the response-building and search responsibilities are extracted into separately-testable, injected services
- [ ] #3 BaseHandler line count is materially reduced and no longer mixes unrelated responsibilities
- [ ] #4 All existing unit tests pass after each extraction batch (no behavior change)
- [ ] #5 New collaborators have direct unit tests
- [ ] #6 Handlers consume collaborators via constructor injection (readonly fields)
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
METHOD DISCIPLINE (2026-09-11, from the TDD question): this is a characterization-first refactor, NOT classic TDD - behavior must not change, so tests are written against CURRENT behavior and are green on arrival. Per extraction step: (1) census the behaviors of the member(s) about to move; identify thin coverage in the ~3610-test suite (BaseHandler is tested mostly indirectly through handler tests); (2) write characterization tests for the gaps FIRST, green on the current code; (3) extract the collaborator; (4) suite stays green with ZERO expectation edits; (5) red-green applies ONLY to new invariants the refactor introduces (the JF-539 Times.Once precedent: graft the new test onto pre-refactor code and watch it fail). No big-bang decomposition: one collaborator per dispatch, one merge per gate.

DECOMPOSITION CENSUS (batch 1, 2026-09-15; BaseHandler was 5569 lines / ~88 protected+private-protected members + 6 consts + 2 enums before batch 1). Line spans are pre-batch-1. Cluster table:

A. SPEECH/SSML OUTPUT - 8 members (TellSsml, AskSsml x2, GetSsml, BuildOutputSpeech, AskLocalized, BuildNowPlayingSpeech, EscapeXml + private EscapeStringArgs), was lines ~2203-2361 + ~4110-4122. ALL STATIC, zero instance state, pure functions of (ResponseStrings, args). Called from BaseHandler itself (~27 refs) + 16 non-BaseHandler production files (14 intent handlers + DisambiguationHelper + ListPaginationHelper) + tests. STATUS: EXTRACTED in batch 1 (2026-09-15) to Alexa/Util/SpeechBuilder.cs (191 lines, public static class); 17 production files + 2 test files migrated mechanically; BaseHandler 5569 -> 5394 lines. Already had SsmlResponseTests (15 facts); AskLocalized was the one thin-coverage member and got 2 characterization tests BEFORE the move. Static-class choice (not ctor injection) deliberate: members were already static and pure, so injection would change every handler ctor for zero testability gain; AC#6 ctor injection deferred to when a STATEFUL collaborator lands (see clusters B/C/H).

B. ANNOUNCE GATING - 6 members: AttachAnnounceIfEnabled (~2372), BuildVideoLaunchSpeech x2 (~2396/2410), SpeakVideoLaunchAnnounceAsync (~2651), GetAnnounceNowPlaying (~3192), GetAnnounceAudioPlays (~3210). Mix: per-user/global config resolution (Plugin.Instance + _config fallback) + speech composition. Candidate collaborator: AnnouncePolicy (instance state = config access; split config getters from speech assembly - speech side already delegates to SpeechBuilder).

C. PLAYBACK LAUNCH + MEDIUM RESOLUTION - ~16 members: ResolveVideoAppStreamDecision (769), PlayingMedium enum + IsVideoAppMedium + ResolvePlayingMedium (808-930), BuildVideoAppTransportRefusal (931), BuildVideoAppLaunchResponse(Async) (985/1045), BuildChannelLaunchResponseAsync (1089), BuildAudioPlayerResponse (1625), AudioLaunchSource record + ResolveAudioLaunchSource + ResolveResumedAudioLaunch + RoutesToAudioTranscode + GetActiveLaunchBaseMs (1751-1960), IsActivelyPlaying (2453), GetVideoAppForAudio (3230). Heavy user-mix coupling: reads DeviceQueueManager, session state, config; hands off to SpeechBuilder for speech. Candidate: ResponseFactory/LaunchResponseBuilder (largest cluster, needs its own dispatch; some members read queue state so design ctor-injected).

D. RESUME/POSITION MATH - 12 members: SortAndFindResumeIndex (300), FindResumeTrackIndex x2 (4039/4052), FindLastPlayedItemWithProgress (3756), ComposeItemAbsolutePosition (1961), ComposeEventPositionTicks (2002), TryGetRuntimeTicksForGuard (2027), GetAudiobookBookKey/GetAudiobookStartTicks (2077/2089), FormatPosition (4295), FormatTimeSpan (4310), BuildPositionDisplay (4329). Nearly all STATIC pure math over (items, ticks); InvariantCulture-sensitive formatting. Candidate: ResumeMath/PositionFormatter static helper - batch-2-ready (pure, easily characterized).

E. SEARCH/FUZZY CORE - ~10 members: SafeGetItemsResult (2783), SearchWithAsrFallbackAsync (2810), CachedSearchAsync (2852), FuzzyMatch/FuzzyMatchPhonetic (2890/2915), GetArtistSongsAsync (2956), SearchItemsFuzzyAsync (3003), GetSearchResponseMode (3087), HandleFuzzyMiss + FuzzyMissOutcome enum (3246-3404). Uses Logger + config thresholds; HandleFuzzyMiss is the big multi-turn decision block. Candidate: SearchService (AC#2's second named responsibility). NOTE JF-382: the inline 4-tier search duplication in PlayArtistSongsIntentHandler must consolidate INTO this, not the reverse.

F. CROSS-MEDIA FALLBACK - 9 members: FindBestNonEmbeddedMatch (125), PassesCrossMediaWordGuard (173), GetCrossMediaArtistSuggestion (3121), BuildCrossMediaArtistOfferAsk (3149), BuildArtistSongsResponseAsync (4369), BuildSingleSongResponse (4495), TrySongFallback (4534), PassesArtistMatchAcceptance (4649), TryEntityFallbackAsync (4709). All instance (Logger + config thresholds + search coupling). Candidate: CrossMediaFallback service, AFTER E exists (it is E's biggest consumer).

G. ALBUM/PLAYLIST PLAY - 5 members: MinFuzzyAlbumQueryLength const, TryStripLeadingAlbumCallingWord (4965), BuildAlbumQuery (5017), TryAlbumFallbackAsync (5103), BuildAlbumPlayResponseAsync (5221), BuildPlaylistPlayResponseAsync (5362). Instance (session/library/config); candidate: AlbumPlayService or fold into C/F dispatch.

H. QUEUE/RADIO/SHUFFLE/PROGRESS REPORTING - ~10 members: FavoritesAndRatingsFirst (248), Shuffle/ShuffleCopy/ShuffleAndCap (3405-3436, static), MirrorQueueToSession (3457), ReportPlaybackProgress (3513), ApplyRepeatModeAsync (3544), FindRadioTracksAsync/FindRadioTracksByGenreAsync (3583/3612), GetPostPlayBehavior (3103), ReportStopOrderedAsync (2725), RunFireAndForget (2693). Candidate: ProgressReporter + RadioSource; ReportPlaybackProgress/ReportStopOrderedAsync touch SessionManager (stateful, ctor-inject).

I. APL DIRECTIVES - 3 members: TryAttachListDirective/TryAttachCarouselDirective/TryAttachNowPlayingDirective (4159-4280, private protected). Instance (Logger); candidate AplDirectiveFactory, small standalone batch.

J. ELICIT/DIALOG RESPONSES - 3 members: BuildElicitSlotResponse (1322), BuildDialogElicitResponse (1367), BuildCancelDuringOpenElicit (1394). First two STATIC + pure; BuildCancelDuringOpenElicit reads session state. Could join SpeechBuilder's neighborhood or a DialogResponses static helper.

K. SESSION/CONFIG GATES + INFRA - ~16 members: GuardIndexReady x2 (218/225, static one-liners), GetLocale (2423, static), IfFeatureDisabled (2464), FilterByContentAccess (2486, static), IfMediaTypeDisabled (2528), IsMusicEnabled (2554), ApplyLibraryFilter (2565, static), SendProgressiveResponse (2595, virtual - pipeline coupling), RetryAsync (2771), ResolveJellyfinUser (4136, static), ResolveSeriesForPlaybackAsync/GetNextUpEpisodesAsync/PlayNextUpEpisodeAsync (3827-4038, TV flow - arguably cluster L). Config gates (IfFeatureDisabled/IfMediaTypeDisabled/IsMusicEnabled + the Get*Behavior getters) belong with the future ConfigPolicy collaborator; TV-nextup trio is its own mini-domain.

L. UNCLASSIFIED SINGLES: GetArtistSubtitle (4280, static, 1-purpose), CheapDtoOptions (5001, static one-liner), PopularitySort (71), CrossMedia* consts. Leave near consumers unless a cluster claims them.

COVERAGE NOTES: A was already directly tested (SsmlResponseTests); D (resume math) and the static parts of H (Shuffle*) have partial direct tests; B/C/E/F are covered only indirectly via handler tests - characterization FIRST for those in their own batches. BATCH 2 RECOMMENDATION: cluster D (ResumeMath/PositionFormatter) - static, pure, InvariantCulture-sensitive (matches the batch-1 risk profile), or cluster I (APL directives) if a smaller step is wanted before the stateful ResponseFactory (C).

BATCH-1 GATE FINDINGS APPLIED (2026-09-15, /simplify 2 combined agents covering 4 angles + code-review high opus, verdict CLEAN with P3s): SpeechBuilder dead using dropped + formatting fixed; AskLocalized test strengthened (exactly-once escape: Contains 'Rock &amp; Roll' + DoesNotContain raw + XDocument.Parse); census count corrected (16 non-BaseHandler files). CENSUS CORRECTIONS for the NEXT batches: (1) cluster D purity was OVERSTATED - FindLastPlayedItemWithProgress is static but I/O (ILibraryManager+IUserDataManager), TryGetRuntimeTicksForGuard is instance+I/O+Logger, ComposeItemAbsolutePosition/ComposeEventPositionTicks are queue-state-coupled: batch 2's ResumeMath must be scoped to the GENUINELY PURE subset only (SortAndFindResumeIndex, FindResumeTrackIndex x2, GetAudiobookBookKey/GetAudiobookStartTicks, FormatPosition, FormatTimeSpan, BuildPositionDisplay) and the manager-touching members deferred to the stateful H/C batches; (2) cluster E carries the JF-408-protected auto-play predicates in HandleFuzzyMiss (FairComparisonScore symmetric comparison, AlternativeFullNameThreshold, the JF-377 downgrade) - two prior relocation attempts were REVERTED (fuzzy_recall_vs_judgment_layers memory): the SearchService dispatch MUST keep the judgment predicates at their decision points or it repeats the reverted mistake; (3) cluster J (BuildDialogElicitResponse/BuildElicitSlotResponse) is NAME-COUPLED to validate_interaction_models.py Phase 8's method-name scan: keep the names or patch the validator in the same change; (4) FOLLOW-UPS filed: batch-1b TellLocalized (the Tell-shape GetSsml-then-wrap pattern survives at ~7 sites + 8 hand-rolled <speak> wraps; SpeechBuilder is the natural home) and cluster-B's BuildNowPlayingSpeech announceOn=true DEFAULT (a policy default inside the util; make the parameter required when cluster B lands - no production site relies on it today). AC#2/AC#6 status: batch 1 delivered separately-testable but NOT injected (static); they close with the cluster-C ctor-injected ResponseFactory and cluster-E SearchService. DO NOT close JF-315 on batch 1's strength.

BATCH 2 STATUS (2026-09-15): cluster D extracted to Alexa/Util/ResumeMath.cs (312 lines, public static; the 8 gate-scoped pure members incl. the exclusively-owned private helpers; the 4-tuple SortByRating stayed with cluster H). BaseHandler 5394 -> 5110 lines. Characterization-first honored: 33 tests written green on pre-move code via a probe subclass, then proven multiset-identical on the move; 26 call sites migrated; zero expectation edits mechanically proven. Purity caveats documented in the ResumeMath class doc: GetAudiobookStartTicks reads the Plugin.Instance tracker singleton, FindResumeTrackIndex creates-on-read via GetOrCreateQueue (parameterization pointer to clusters H/C). Suite 3861/3861 both TFMs; Release 0 warnings; validators PASS. Gates: /simplify 4-angle pass (dead using, blank lines, sorted usings, WithDeviceQueue dedup, CreateJellyfinUser helper replacing the 44th test-user copy) + review-local 5-reviewer pass (nothing >=80 on the code; byte-identity independently re-verified). Filings: JF-570 (twin SortByRating split, lands with cluster H) and JF-571 (the CreateJellyfinUser dedicated batch). BATCH 3 recommendation: cluster I (APL directives, 3 members) or batch-1b TellLocalized; cluster C (ctor-injected ResponseFactory, closes AC#2/#6) is batch 4.

BATCH 3 STATUS (2026-09-15): BOTH pieces landed. (1) CLUSTER I: TryAttachListDirective + TryAttachCarouselDirective extracted VERBATIM to Alexa/Util/AplDirectiveAttacher.cs (113 lines, internal static because ListDisplayItem is an internal record; ILogger as first parameter - the SpeechBuilder/ResumeMath static precedent). Statement-identity of both bodies proven mechanically vs git HEAD (Logger->logger receiver swap only; independently re-verified by the review gate with commit archaeology back to JF-177/54cf1965). TryAttachNowPlayingDirective STAYED in BaseHandler per the dispatch's config guard: it calls GetImageUrl (reads _config.ServerAddress + user token), so it is NOT Logger-only state; rationale documented at both ends, revisit with cluster C. 4 call sites migrated (InProgressMediaList, QueryRecentlyAdded, QueryArtistLibrary + added Util using, BrowseLibrary). 11 characterization facts written FIRST, green on pre-move BaseHandler code via a probe subclass, then retargeted to the static home; the thin spots pinned were the interactive-APL session flip + CarouselReprompt injection (incl. reprompt-preserved and no-injection-on-open-session nuances) and the empty-items null-build path (no attach AND no flip). The 3 pre-existing TryAttachCarouselDirective facts moved from Unit/AplHelperTests.cs into the new Unit/AplDirectiveAttacherTests.cs with assertions mechanically proven identical (zero expectation edits). (2) BATCH-1b TellLocalized, CENSUS CORRECTION: the gate's "~7 Tell-shape sites" split into (a) 5 OutputSpeech-shape sites (GetSsml with pre-escaped args + raw-args plain fallback, ternary-wrapped, assigned into existing responses) which are EXACTLY the existing SpeechBuilder.BuildOutputSpeech contract - migrated byte-identically: PlayBook resume x2, FollowMe, Recommend movie announce, HandleFuzzyMiss qualifier (byte-identity pinned by BuildOutputSpeech_ByteIdenticalToPreMigrationHandRolledShape, both branches); and (b) the actual full-Tell ternaries (MediaInfoIntentHandler x2) whose SSML args are pre-escaped or pre-composed SSML-ready content that any internally-escaping single-args helper would double-escape - they stay hand-written on GetSsml+TellSsml. SpeechBuilder.TellLocalized landed as dispatched (Tell sibling of AskLocalized, composed over BuildOutputSpeech after the /simplify reuse finding killed the duplicated ternary; ResponseBuilder.Tell(IOutputSpeech) semantics verified against the pinned Alexa.NET 1.22.0 DLL): ZERO production callers today, documented in its doc comment; cluster C MUST adopt-or-delete it. Sites left, with reasons: MediaInfo x2 (pre-escaped/pre-composed args), Recommend audio branch ~217 (GetAnnounceNowPlaying gate + silent-on-SSML-miss: BuildNowPlayingSpeech migration would ADD a plain fallback = behavior change, needs its own decision), PlayRadio (message concatenation inside both paths), LaunchRequest Welcome (dual-SSML Ask, the batch-1 documented exception), SetReminder (reminder alert SSML, not a response), BaseHandler ~2361 announce conversion (cluster B). BaseHandler 5110 -> 5022 lines. Suite 3875/3875 both TFMs (baseline 3861 + 14 new facts); Release 0 warnings; validate_interaction_models PASS; no cluster-J elicit names in the diff. Gates: /simplify 4-angle pass (applied: dead probe mock + dead usings x7 in the new test file, TellLocalized composed over BuildOutputSpeech, NowPlaying stay-note deduped to a pointer, XDocument.Parse wart; skipped: rewriting the 3 verbatim-moved carousel facts - zero-expectation-edit discipline) + review-local 5-reviewer pass (verdict CLEAN, nothing >=80; P3 doc-wording nits applied; the one pre-existing observation - PlayBook resume ShouldEndSession=true on a VideoApp-shaped response - is ALREADY TRACKED as JF-567 item 4). Follow-ups tracked HERE as JF-315 residue: DisambiguationHelper's hand-rolled carousel attach (token "disambiguation") and ListQueueIntentHandler's gate+build+attach block should migrate to AplDirectiveAttacher in a later verbatim-move batch; the Recommend audio-branch decision above. BATCH 4 recommendation: cluster C (ctor-injected ResponseFactory) - closes AC#2/#6, absorbs GetImageUrl + TryAttachNowPlayingDirective naturally (config-dependent URL resolution gets its proper home), and is TellLocalized's first chance at a production caller.

BATCH 4+5 STATUS (2026-09-15, from the commit messages, 94d7b6d6 + 3254cdc9): cluster C landed as the ctor-composed PlaybackLaunchBuilder (audio-launch core batch 4, the VideoApp launch family batch 5; the SendProgressiveResponse virtual-method-group delegate seam; TellLocalized still has zero production callers after batch 5 - both refusal keys are plain-text-only). BaseHandler 4284 -> 3712 across the two batches; PlaybackLaunchBuilder 1434 lines; suite 3905/3905 both TFMs.

BATCH 6 STATUS (2026-09-16): cluster E extracted to Alexa/Util/SearchService.cs (public sealed; composed on BaseHandler as the protected-internal readonly `Search` property beside `Launch`, ctor-initialized with (config, Logger, AlexaRequestTimeoutMs) - the budget const stays PRIVATE on BaseHandler and is composition-passed, the PlaybackLaunchBuilder delegate-seam precedent, so Util holds no Handler-layer reference; see JF-572 for the const's true-home consolidation). 8 members moved VERBATIM, byte-identity mechanically proven vs git HEAD across all bodies modulo the declared swaps (Logger->_logger, protected->public, the ApplyLibraryFilter forwarder inlined to its Util.LibraryFilter target with identical arguments, one doc-cref namespace fix, plus a doc-only dead-code note): SafeGetItemsResult, SearchWithAsrFallbackAsync, CachedSearchAsync, FuzzyMatch, FuzzyMatchPhonetic, GetArtistSongsAsync, SearchItemsFuzzyAsync, GetSearchResponseMode. SearchService gained a TWIN private RetryAsync (the original stays for BaseHandler's own callers; the JF-570 twin-SortByRating precedent, consolidation filed as JF-572) so moved bodies keep their verbatim shape. THE DECISION-BLOCK STAY (the dispatch's critical constraint): HandleFuzzyMiss + FuzzyMissOutcome did NOT move. Rationale lives ONCE in the SearchService class doc, with pointers from the Search property doc and the HandleFuzzyMiss <remarks> stay-note: it is the auto-play DECISION block (JF-508/JF-526 coverage auto-accept, the AutoPlay disjunct, the ContainmentScore no-qualifier bar) and the JF-408 fuzzy-recall-vs-judgment-layers memory (two REVERTED relocation attempts) keeps judgment at its decision point while SearchService owns only recall (the class doc also reconciles the one pre-existing judgment inside a moved body: SearchItemsFuzzyAsync's coverage gate arrived verbatim; relocating it would be a JF-408-class decision). It is also seam-coupled to Launch.SpeakVideoLaunchAnnounceAsync (the virtual SendProgressiveResponse dispatch ~15 test harnesses override). CENSUS CORRECTION: the census listed "FairComparisonScore symmetric comparison, AlternativeFullNameThreshold, the JF-377 downgrade" as living in HandleFuzzyMiss - they do NOT; they live on PlayArtistSongsIntentHandler's gate and ArtistSearch, and they were therefore untouched. Call sites migrated with Search. receiver prefixes, each proven a pure prefix insertion: BaseHandler x6 (the series fuzzy fallback, the album-track SafeGetItemsResult pair, the playlist GetItems + playlist fuzzy fallback + playlist FuzzyMatch pre-check), 12 handler files (27 lines), and 3 test probe forwarders (zero expectation edits mechanically shown in the diff). Characterization-first: facts written green on the PRE-move code via a probe subclass, then the file retargeted per the /simplify pass to construct SearchService directly (plus one new wiring invariant fact, Search_Property_Wired_By_BaseHandler_Ctor); pinned the previously-covered-by-NOTHING spots: SafeGetItemsResult's NRE fallback, CachedSearchAsync's three branches (it is DEAD in production today - zero callers - flagged in its doc for a deletion follow-up rather than deleted in a no-behavior-change batch), GetSearchResponseMode resolution, FuzzyMatchPhonetic null-index degradation, and the two query shapes (the JF-358 IncludeItemTypes=Audio invariant; note InternalItemsQuery initializes MediaTypes to an empty array, not null). FuzzyMatch threshold resolution was pinned then DROPPED as a duplicate of FuzzyMatchConfigurationTests (reuse-review finding). JF-382 CONSOLIDATION DECISION: DEFERRED to batch 6b per the dispatch's escape hatch. The inline 4-tier search in PlayArtistSongsIntentHandler (lines ~219-480) interleaves mode policy (Fast/Thorough/Parallel), the pinned-index capture, per-tier JF-numbered logging, the JF-417 tier-2 deferral, and the JF-381 gate exceptions - consolidating it into SearchService (or onto ArtistSearch.SearchAsync) is a behavioral-risk step, not mechanical re-pointing; the handler's calls to the moved members were re-pointed (Search. prefix) so 6b's starting point is unchanged. Gates: /simplify 4-angle pass (applied: CreateSong reuse replacing a 4th private Audio factory, probe collapse to direct construction + the wiring fact, duplicate FuzzyMatch fact dropped, unused usings, stay-rationale deduped to one owner + pointers, RetryAsync doc corrected from "moved" to "copied", budget ctor-passed killing the Util->Handler const arrow; skipped: nothing) + JF-572/JF-573 filed same-turn for the deferred consolidations. BaseHandler 3712 -> ~3400 lines. Suite final counts in the commit; Release build 0 warnings; validate_interaction_models PASS. NEXT: batch 7 = cluster F (CrossMediaFallback, AFTER E per the census) or cluster B (AnnouncePolicy); 6b = the JF-382 consolidation + the CachedSearchAsync deletion decision.

BATCH 7 STATUS (2026-09-16): cluster F extracted to Alexa/Handler/CrossMediaFallback.cs (903 lines, public sealed; composed on BaseHandler as the protected-internal readonly `CrossMedia` property beside `Search`/`Launch`, ctor-initialized with (config, Logger, Launch, AlexaRequestTimeoutMs) - the budget const composition-passed like batch 6, and Launch as a ctor param per the dispatch so the play-shape sinks never call back into the handler; NO Search param needed: on reading, the moving bodies call the STATIC recall utils (FuzzyMatcher/ArtistSearch/KeywordMatcher) directly, not SearchService instance members). NAMESPACE DEVIATION FROM THE LAUNCH/SEARCH PRECEDENT, deliberate and documented in the class doc: the collaborator lives in the HANDLER namespace, not Util, because BuildCrossMediaArtistOfferAsk is wired into DisambiguationHelper (a Handler-namespace internal static with 16 consumer files); zero Util files reference the Handler namespace today and this batch kept that invariant. 11 members moved VERBATIM (byte/statement-identity mechanically proven vs git HEAD with a comment/string-aware extractor, all bodies identical modulo the declared swaps: Logger->_logger incl. 2 bare-argument positions, Launch.X->_launch.X, the ApplyLibraryFilter forwarder inlined to Util.LibraryFilter with identical arguments, protected->public, protected static->private static for the twins, one doc-comment em-dash sentence restructured): the 9 roster members (FindBestNonEmbeddedMatch, PassesCrossMediaWordGuard, GetCrossMediaArtistSuggestion, BuildCrossMediaArtistOfferAsk, BuildArtistSongsResponseAsync, BuildSingleSongResponse, TrySongFallback, PassesArtistMatchAcceptance, TryEntityFallbackAsync) + CountContentWords (private helper) + ApplyAnnouncement (internal static; the one remaining BaseHandler caller, cluster G's BuildAlbumPlayResponseAsync, re-points with the receiver prefix). THREE helpers moved WITH their consumers as single definitions: CrossMediaArtistThreshold + CrossMediaArtistMaxWords + CrossMediaSongThreshold consts (only staying refs were 2 doc crefs, re-pointed) and PopularitySort (the one BaseHandler copy deleted; PlayArtistSongs x2 + QueryArtistLibrary call sites re-pointed to the static - NOTE the census grep lesson: QueryArtistLibrary's caller was found only by a post-migration full re-grep, the initial census grep was head-truncated). THREE private twins, all documented copied-not-moved with their consolidation homes: RetryAsync (JF-572 scope EXTENDED same-turn to name the third twin + the Shuffle twin), Shuffle (consolidation belongs to cluster H's family), IsMusicEnabled property (expression-identical JF-467 live read; BaseHandler keeps its own for the album cascade). JF-408 DISCIPLINE honored: every auto-play DECISION predicate (the JF-382 end-gates, the JF-363 band precedence, PassesArtistMatchAcceptance) moved WITH its call site as verbatim code - nothing needed to be left behind because none of these decisions lived outside the moved bodies (the JF-420-family gates were already on PlayArtistSongs, untouched). Call sites migrated: BaseHandler x3 (TryAlbumFallbackAsync's word-guard + walk-down, BuildAlbumPlayResponseAsync's announce), 9 handler files (13 lines), the SharedGateProbeHandler forwarder in TestHelpers, PlaySongIntentHandler's doc cref; CLAUDE.md's Cross-Media-Type Fallback section re-pointed same-change (the batch-4/6 precedent; caught by the review-local history reviewer). Characterization-first: 19 facts written green on the PRE-move code via a probe subclass, then retargeted to construct CrossMediaFallback directly + 1 wiring fact (CrossMedia_Property_Wired_By_BaseHandler_Ctor, via the shared SharedGateProbeHandler after the reuse-review finding killed the per-file probe); pinned the previously-direct-coverage-zero spots: the JF-412 walk-down loop (skip embedded, next eligible, exhaust to null), the JF-479 word-level count vs fragment tokens, the JF-363 per-user/global resolution, the offer-ask session-attribute wiring (both decline types), the JF-471 plain/phonetic branches (cup->Koop floor >= 91, length-band score-0), and the JF-345 override; the big instance members keep their handler-level coverage (CrossMediaTypeFallbackTests et al.) per the batch-6 scoping precedent. BaseHandler 3415 -> 2668 lines (-747). Suite 3939/3939 BOTH TFMs (baseline 3919 + 20 new facts, ZERO expectation edits); Release solution build 0 warnings both TFMs; validate_interaction_models PASS. Gates: /simplify 3-agent pass (applied: shared-probe reuse, 4 dead usings x2 files, IsMusicEnabled twin doc's remaining-caller claim corrected, ctor doc 'no BaseHandler reference' phrasing, paired #pragma restore CA1859, BaseHandler property stateless-list; skipped: nothing) + review-local 5-reviewer pass (bug scan CLEAN with independent byte-identity re-verification; history reviewer's CLAUDE.md doc-drift finding applied same-turn; PR-comment reviewer CLEAN-NO-PRS; no finding >= 80 survived scoring). JF-572 extended same-turn (third RetryAsync twin + Shuffle twin + the composition-passed budget note). NOT COMMITTED (per dispatch). NEXT: batches G (album/playlist play - TryAlbumFallbackAsync/BuildAlbumPlayResponseAsync/BuildPlaylistPlayResponseAsync + their BuildAlbumQuery/CheapDtoOptions helpers; note ApplyAnnouncement now lives on CrossMedia), B (AnnouncePolicy), H (queue/radio/shuffle/progress - owns the Shuffle-twin consolidation and JF-570's SortByRating); 6b = JF-382 consolidation + CachedSearchAsync deletion.
<!-- SECTION:NOTES:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [ ] #1 dotnet build passes with 0 errors
- [ ] #2 dotnet test passes
- [ ] #3 No new compiler warnings introduced
- [ ] #4 Session attributes use proper DTOs not raw ValueTuples for serialization
- [ ] #5 HttpClient instances are not shared across calls that modify BaseAddress
- [ ] #6 NLU test fixtures updated if interaction model changed
- [ ] #7 E2E test added for new intent or handler logic
- [ ] #8 Locale response strings added to all 17 locales
- [ ] #9 /simplify passed (no blocking cleanups remaining)
- [ ] #10 /code-review high passed (no blocking findings remaining, or findings applied/tracked)
<!-- DOD:END -->
